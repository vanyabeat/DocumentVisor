<Window x:Class="DocumentVisor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:view="clr-namespace:DocumentVisor.View"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="DocumentVisor" Height="720" Width="1280">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="/Resources/StringResource.xaml" ></ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <TabControl>
            <TabItem Header="{StaticResource Queries}">
                <view:QueryView></view:QueryView>
            </TabItem>
            <TabItem Header="{StaticResource Persons}">
                <view:PersonsControl></view:PersonsControl>
            </TabItem>
            <TabItem Header="{StaticResource PersonTypes}"/>
            <TabItem Header="{StaticResource Privacies}"/>
            <TabItem Header="{StaticResource Themes}"/>
        </TabControl>
    </Grid>
</Window>